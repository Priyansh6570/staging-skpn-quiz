import QuizScreen from "@/components/QuizScreen";

// The "your attempt is already recorded" state. The export had no route for it.
export default function QuizPage() {
  return <QuizScreen phase="done" />;
}
